
import React from 'react'
import Option from './Option'

describe('Option', () => {
  it('should render Option', () => {
    const props = {

    }
    global.shallowSnapshot(<Option {...props} />)
  })
})
