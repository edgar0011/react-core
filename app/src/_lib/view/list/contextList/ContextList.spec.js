// TODO: move to core-fe
import React from 'react'
import ContextList from './ContextList'

describe('ContextList', () => {
  it('should render ContextList', () => {
    const props = {

    }
    global.shallowSnapshot(<ContextList {...props} />)
  })
})
